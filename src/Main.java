/**
 * This program implements a reminder app, which is built as a calendar.
 * You can add one reminder for every date in the calendar.
 * To launch the app use 'CalendarApp.run' method.
 * Make sure to create a new file (or open an existing one) before trying
 * to add new reminders. The app accepts only '.txt' files.
 */

public class Main {
    public static void main(String[] args) {
        CalendarApp.run();
    }
}

